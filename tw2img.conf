# tw2img default configuration
# Location: ~/.config/tw2img/tw2img.conf
#
# All CLI flags can be set here as defaults.
# CLI flags always override these values.

[tw2img]

# Output
# Default directory to save rendered PNGs (leave commented to use current working directory)
#output_dir = ~/Pictures/tweets

# Duplicate file handling
# Controls what happens when the output file already exists.
#
#   overwrite  (default) - silently replace the existing file, same as before
#   increment  - append a counter suffix: photo.png -> photo-1.png -> photo-2.png ...
#   epoch      - append the current Unix epoch (seconds): photo.png -> photo-1779464539.png
#
#duplicate_files = overwrite

# Default viewer
# kitten icat (render directly in kitty term), viewnior, eog, firefox, etc
# firefox - when used with --save-html the tweet will load directly in the
# browser, bypassing playwright to create an image
#viewer = kitty +icat

# Default article viewer
# Due to the length of output and urls, it's best to render HTML and view in browser
#article_viewer = firefox

# Default output width in pixels (tweet card width)
width = 598

# Render in light mode instead of dark
light = false

# Disable @2x retina (half-size output)
no_retina = false

# Content display
# Hide the "Posted from iPhone" / source label
no_source = false

# Show only the focal tweet, strip parent thread context
no_context = false

# Show full unabbreviated stat counts (e.g. 12,345 instead of 12.3K)
full_stats = false

# Use Nitter CSS theme instead of Twitter default dark/light
nitter = false

# Authentication
# Use guest mode (no auth tokens required, limited API access)
guest = false

# Include the user's own replies when fetching @user timeline
# Requires authentication; silently ignored in guest mode.
# Set to true to include replies alongside original tweets.
with_replies = false

# Auth tokens - prefer environment variables TWITTER_AUTH_TOKEN / TWITTER_CSRF_TOKEN
# Uncomment and set here only if you don't use envars
#auth_token = your_auth_token_here
#csrf_token = your_csrf_token_here

# Upload / export
# Automatically upload rendered PNG to Imgur
imgur = false

# Append imgur URL and delete link to this file after each upload
# Leave commented out to disable
#imgur_log = ~/tw2imgur_urls

# Advanced
# Path to a custom Nitter CSS theme file (overrides --nitter)
#css = /path/to/nitter/public/css/themes/pleroma.css

# Save HTML instead of rendering PNG (leave blank to disable)
#save_html =

# Dump raw API JSON to stdout and exit (debugging)
dump_json = false

# Print a one-line text summary of the focal tweet to stdout
print_line = false

# Translation (optional - requires: pip install deep-translator)
# Translate tweet text before rendering.
# Format: [SOURCE:]TARGET
#   TARGET only       -> auto-detect source language, translate to TARGET
#   SOURCE:TARGET     -> explicitly set both source and target languages
# Language codes use BCP-47 / ISO 639-1 (e.g. en, ja, fr, de, zh-CN).
# Use "auto" as the source to let Google Translate detect the language.
#
# Examples:
#   trans = en          # auto-detect -> English
#   trans = ja:en       # Japanese -> English
#   trans = auto:fr     # auto-detect -> French
#
# Leave commented to disable translation entirely.
#trans =
